'use strict';

const test = require('ava');
const utils = require('../lib/utils');

test('extracting hashtags from text', t => {
  let tags = utils.extractTags('a #picture with tags #AwesOme #Platzi # ava #100 ##yes');

  t.deepEqual(tags, ['picture', 'awesome', 'platzi', 'ava', '100', 'yes']);

  tags = utils.extractTags('a picture with no tags');
  t.deepEqual(tags, []);

  tags = utils.extractTags();
  t.deepEqual(tags, []);

  tags = utils.extractTags(null);
  t.deepEqual(tags, []);
});
//# sourceMappingURL=..\node_modules\.cache\ava\f8986073019b4e4aa3c027f8d79652e4.js.map